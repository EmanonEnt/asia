# LiveGigs Editor v8.8.0 更新说明

## 主要更新

### 1. 分享按钮修复
- **问题**：FUJI ROCK FESTIVAL'26和NEW YEAR PARTY等海报的分享按钮无法点击
- **修复**：移除了分享按钮的过期活动限制，现在所有活动都显示分享按钮
- **CSS修复**：添加了`pointer-events: auto !important`和`z-index: 1000 !important`确保按钮可点击

### 2. UPLOAD EVENT按钮
- 在海报滚播区域下方添加了"UPLOAD EVENT"按钮
- 点击后弹出上传表单

### 3. 微信支付
- 支付二维码路径：`./image/Wechat-QR.png`
- 请将微信收款二维码保存为`Wechat-QR.png`放到网站的`./image/`目录下
- 支持自动货币检测（根据用户时区和语言）

### 4. 客户上传管理
- 新增"客户上传"标签页
- 功能：
  - **刷新列表**：刷新显示最新的上传数据
  - **导出数据**：将上传数据导出为JSON文件
  - **导入数据**：从JSON文件导入上传数据
  - **手动添加**：手动添加新的上传记录
  - **允许**：将活动添加到滚动海报列表
  - **不允许**：拒绝上传（需手动退款）
  - **删除**：删除上传记录

## 数据同步说明

由于浏览器localStorage在不同域名之间不共享，采用JSON文件导入导出方式同步数据：

### 前端用户上传流程：
1. 用户点击"UPLOAD EVENT"按钮
2. 填写活动信息并支付
3. 数据保存在用户浏览器的localStorage中

### 管理员审核流程：
1. 管理员在自己的浏览器打开编辑器
2. 切换到"客户上传"标签页
3. 点击"刷新列表"查看是否有新上传
4. 如果看不到数据，需要用户或管理员导出JSON文件
5. 点击"导入数据"选择JSON文件
6. 审核通过后点击"允许"，活动将添加到滚动海报列表

### 数据导出/导入：
- **导出**：点击"导出数据"按钮，下载`pending_uploads_YYYY-MM-DD.json`文件
- **导入**：点击"导入数据"按钮，选择JSON文件导入

## 文件命名

编辑器文件名为 `LiveGigs-Editor.html`（不锁定版本号）

## 部署说明

1. 将 `LiveGigs-Editor.html` 上传到GitHub
2. 将 `Wechat-QR.png` 放到 `./image/` 目录下
3. 管理员从GitHub下载最新编辑器进行操作

## 微信支付二维码

请将微信收款二维码保存为 `Wechat-QR.png`，放到网站根目录的 `image` 文件夹中。

路径：`./image/Wechat-QR.png`
