兼容版部署说明

你现在的代码：
<script src="./js/data-loader.js"></script>
<script>DataLoader.init('cn');</script>

新版本的 livegigs-data.js 完全兼容！

部署步骤：
1. 删除旧的 asia/js/data-loader.js
2. 上传新的 livegigs-data.js 到 asia/js/ 目录
3. 上传 admin/ 文件夹到 asia/admin/
4. 完成！页面会自动从GitHub获取数据

数据流向：
后台保存 → GitHub存储 → 前台DataLoader.init() → 从GitHub获取 → 触发事件 → 页面更新

注意：
- 保持现有的 <script>DataLoader.init('cn');</script> 代码不变
- 新的 livegigs-data.js 会自动从GitHub获取数据
- 通过事件触发页面更新
